interface:
  display_name: "FFmpeg Canvas"
  short_description: "Transform media with scoped FFmpeg arguments"
  default_prompt: "Use $ffmpeg-canvas to transform the media I provide, choosing managed Canvas execution when available or a user-authorized local FFmpeg route."
