interface:
  display_name: "FFmpeg Canvas"
  short_description: "Transform and split media with scoped FFmpeg"
  default_prompt: "Use $ffmpeg-canvas to transform the media I provide; when separating audio and video, create both a silent video and an independent audio output."
