{
  "name": "@microvoid/convax-skill-clip-export",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "bun ../../../tooling/validate.mjs --kind skill --id clip-export",
    "pack": "bun ../../../tooling/pack.mjs --kind skill --id clip-export"
  }
}
